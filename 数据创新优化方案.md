# 数据创新优化方案

> 基于课题文档「数据质量与增强优化」方向设计

---

## 当前数据画像

### 第一轮（自拟文本，已完成录制）

| 指标 | 当前值 | 课题建议 |
|------|--------|----------|
| 样本数 | 100 条 | 20~100 条 |
| 总时长 | 6.3 分钟 | 10~30 分钟 |
| 单条时长 | 2.0~6.7 秒 | — |
| 采样率 | 24kHz mono | ≥16kHz ✅ |
| 带首尾静音 | **87/100（87%）** | ❌ 待修复 |
| 静音时长 | 0.6~1.4 秒（开头） | ❌ 浪费有效时长 |
| 峰值差异 | 0.23~0.58（差 2.5 倍） | ❌ 音量不一致 |
| 文本来源 | 自拟（日常/校园/AI主题） | — |
| 文本多样性 | 句式较单一，偏陈述句 | ⚠️ |

### 第二轮（标贝标准语料，待录制）

| 指标 | 值 |
|------|-----|
| 样本数 | 100 条 |
| 文本来源 | 标贝 (BZNSYP) 标准中文 TTS 语料库 |
| 字数范围 | 10-33 字 |
| 平均字数 | 17.9 字 |
| 预计总时长 | ~10-15 分钟（业余朗读） |
| 参考音频总时长 | 7.0 分钟（标贝专业配音员） |
| 疑问句 | 12 条 |
| 感叹句 | 8 条 |
| 复句（含逗号） | 61 条 |
| 含数字/量词 | 16 条 |
| 含人名/专名 | 多条 |

**两轮对比优势**：
- 第一轮：内容贴近个人生活，录音风格自然，但句式偏简单
- 第二轮：标贝语料覆盖面广（新闻/故事/对话/正式），句式更丰富，更接近真实 TTS 应用场景

**核心矛盾**：原始 100 条音频中，首尾静音平均占用约 1.6 秒，实际有效语音仅约 **3~5 秒×100 = 5~8 分钟**。87% 的样本存在头尾静音，直接影响模型对语音起止边界的学习。

---

## 方案概览

```
原始数据 (100条 m4a)
    │
    ▼
Phase 1: 预处理流水线 ─── 静音裁剪 → 响度归一化 → 重采样检查
    │
    ▼
Phase 2: 质量筛选 ─────── ASR 转写对齐 → 质检打分 → 过滤坏样本
    │
    ├─ 合格集（预计 80-90 条）
    │       │
    │       ├── 子集 A（30 条）：高置信度样本
    │       ├── 子集 B（50 条）：中等以上样本
    │       └── 全集（80+ 条）：全量训练
    │
    ▼
Phase 3: 数据增强 ──────── 语速扰动 ±10% → 音高扰动 ±50Hz
    │
    ▼
Phase 4: 对照实验 ─────── 5 组 SFT → SIM/CER 评测 → 结论
```

---

## Phase 1: 预处理流水线（自动化）

### 1.1 静音裁剪（Silence Trimming）

**问题**：87 条样本存在 0.6~1.4 秒的首尾静音，占总时长 20%~40%。

**方法**：
- 基于 RMS 能量阈值检测语音起止点
- 阈值设为峰值 RMS 的 -30dB
- 首尾各保留 50ms 过渡空间（避免切太紧导致爆音）
- 裁剪后检查时长 ≥1.5 秒

**工具**：`librosa.effects.trim` 或 `torchaudio.functional.vad`

**预期效果**：
- 去除无效静音，每条节省 0.5~2 秒
- 模型不再学习「静音→语音」的无关模式
- 总有效训练音频净时长可能反而减少，但质量提升

### 1.2 响度归一化（Loudness Normalization）

**问题**：峰值从 0.23 到 0.58，音量差异 2.5 倍，模型可能误将音量当作音色特征。

**方法**：
- 使用 ITU-R BS.1770-4 响度标准（`pyloudnorm`）
- 目标响度：-23 LUFS（广播标准）
- 或简化为：统一 RMS 归一化到 -20dBFS

**工具**：`pyloudnorm` 或 `torchaudio.transforms.Vol`

**预期效果**：
- 消除音量不一致对模型训练的干扰
- 合成语音音量更均匀

### 1.3 可选的轻量去噪

**方法**：
- `noisereduce` 库的频谱门控降噪（基于估计的噪声剖面）
- 降噪力度设为轻度（`prop_decrease=0.5`），避免过度处理导致音色失真

**备注**：去噪是双刃剑——过度去噪可能损伤高频音色特征。建议仅在明显有底噪的样本上使用，并在消融实验中对比开/关去噪的效果。

---

## Phase 2: 质量筛选（半自动）

### 2.1 ASR 转写质检

**方法**：
- 使用 **FunASR Paraformer**（已有依赖）逐条转写音频
- 计算每条样本的 **CER（字错率）**：转写结果 vs 标注文本
- CER > 30% → 疑似文本-音频不对齐，标记为待人工复核

**工具**：`funasr.AutoModel(model="iic/speech_paraformer_asr_nat-zh-cn-16k-common-vocab8404-pytorch")`

### 2.2 构建质检分数

综合打分 = 静音占比(越低越好) + CER(越低越好) + 音量是否正常：

| 质检等级 | 标准 | 预计占比 |
|----------|------|----------|
| A 级（优） | CER < 10%, 静音占比 < 15%, 无其他问题 | ~40% |
| B 级（良） | CER < 20%, 静音占比 < 25% | ~40% |
| C 级（差） | CER > 30% 或静音占比 > 30% | ~20% |

### 2.3 构建对比训练集

从质检结果构造 3 个训练集进行消融：

| 训练集 | 构成 | 预计条数 |
|--------|------|----------|
| `clean_30` | 仅 A 级样本 | ~30 条 |
| `clean_50` | A + B 级，按分数取 top-50 | ~50 条 |
| `full_80` | 全部通过质检的样本（过滤 C 级） | ~80 条 |

---

## Phase 3: 数据增强（可控扰动）

对筛选后的数据集，通过轻度扰动增加训练样本多样性：

### 3.1 语速扰动（Speed Perturbation）

- 使用 `torchaudio.transforms.SpeedPerturbation`（需重采样）
- 或 `librosa.effects.time_stretch`
- 扰动幅度：±10%（0.9x, 1.0x, 1.1x）
- **新增**：每 1 条原始样本 → 3 条样本

### 3.2 音高扰动（Pitch Shift）

- 使用 `librosa.effects.pitch_shift`
- 扰动幅度：±50 Hz（约半音）
- 注意：**仅对训练音频做音高扰动，ref_audio 不变**

### 3.3 限制

- **不做**大范围数据增强（如 SpecAugment 频域掩码）——这属于训练侧优化，不在数据侧
- 扰动后音频需重新经过 Tokenizer 提取 audio_codes（`prepare_data.py`）

---

## Phase 4: 对照实验设计（消融实验）

### 核心创新点：双轮数据对比

**本实验的核心创新**在于对比两种数据来源对 TTS 微调效果的影响：
- **第一轮（自拟文本）**：个人自拟的 100 条日常/校园/AI 主题句子，内容贴近个人但句式较简单
- **第二轮（标贝语料）**：从标贝 10,000 条标准 TTS 语料中分层抽样 100 条，句式丰富、覆盖多种语言现象

### 实验矩阵

| 实验组 | 轮次 | 数据来源 | 预处理 | 增强 | 目的 |
|--------|------|----------|--------|------|------|
| **Baseline-R1** | R1 | 自拟 100 条，未清洗 | — | — | 第一轮基线 |
| **Baseline-R2** | R2 | 标贝 100 条，未清洗 | — | — | 第二轮基线（零样本对比） |
| **Exp-1** | R1 | 自拟 clean_50（裁剪+归一化+质检 A/B） | ✅ | — | 验证第一轮质量潜力 |
| **Exp-2** | R2 | 标贝 clean_50（裁剪+归一化+质检 A/B） | ✅ | — | 验证第二轮最佳配置 |
| **Exp-3** | R1 | 自拟 clean_50 | ✅ | 语速±10% + 音高±50Hz | 数据增强增益 |
| **Exp-4** | R2 | 标贝 clean_50 | ✅ | 语速±10% + 音高±50Hz | 标贝+增强增益 |
| **Exp-5** | R1+R2 | 混合 100 条（各取 50 条最佳）| ✅ | — | 验证数据来源多样性增益 |

### 核心假说

1. **R2(标贝) > R1(自拟)**：标贝语料句式多样性更高，SFT 微调后的 CER 应更低，泛化能力更强
2. **预处理增益**：裁剪+归一化后，CER 应显著改善（因为去除了静音对语音起止边界的误导）
3. **增强增益**：语速/音高扰动提升对未见说话风格的泛化能力
4. **混合 > 单一**：R1+R2 混合训练集可能同时获得 R1 的个人风格自然度和 R2 的泛化鲁棒性

### 统一变量

- SFT 超参：`lr=2e-5, epochs=5~10, batch_size=2, gradient_accumulation_steps=4`
- Tokenizer：`Qwen3-TTS-Tokenizer-12Hz`
- 评测：同一测试文本集（20 条未参与训练的文本）
- 指标：SIM + CER + 主观听感 MOS
- ref_audio：统一使用同一说话人最优质的参考音频

---

## 实施路线（已更新——双轮数据）

```
✅ 已完成: R1 100 条自拟文本 → 录制 → m4a → wav 转换
✅ 已完成: R2 100 条标贝文本 → 分层抽样筛选 → text_v2.txt
⬜ Step 1: R2 录音（按 text_v2.txt 录制 100 条标贝句子）
⬜ Step 2: R2 格式转换（m4a → 24kHz wav） + 生成 train_raw_v2.jsonl
⬜ Step 3: 两轮数据预处理（静音裁剪 + 响度归一化）
⬜ Step 4: 质检打分（ASR 转写 → CER → A/B/C 分级）
⬜ Step 5: 构建 6 组训练集 JSONL
⬜ Step 6: 提取 audio_codes（prepare_data.py）
⬜ Step 7: 分别 SFT 训练（6 组，在 GPU 服务器上）
⬜ Step 8: 评测对比（evaluation/run_sim.py + 主观听感）
⬜ Step 9: 数据分析与报告撰写
```

---

## 创新点总结（报告/答辩用）

1. **双轮数据对比实验**：系统对比「自拟文本」vs「标准 TTS 语料（标贝）」两种数据来源对 SFT 微调效果的影响，从数据多样性的角度解释 SIM/CER 差异，而非单纯比较预处理/超参

2. **数据质量诊断**：建立了音频质量多维度自动扫描流水线（静音/音量/削波/DC），定量分析了数据集的系统性缺陷

3. **「少而精」假说验证**：通过 A/B/C 三级质检 + 多组不同规模的训练集消融，用 SIM/CER 曲线实证了 TTS 微调中数据质量优先于数量的规律

4. **预处理 vs 增强贡献分离**：通过基准组/仅预处理/预处理+增强的对照，定量分析各环节对最终效果的贡献

5. **语料多样性量化**：从字数范围、句法复杂度（疑问/感叹/复句占比）、领域覆盖度等维度量化两组数据的差异，并与最终评测指标关联分析
