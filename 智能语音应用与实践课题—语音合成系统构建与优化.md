讯飞×中科大AI英才班暑期实训营
# 智能语音应用与实践课题：语音合成系统构建与优化

## 课题目标

本课题面向**个性化语音合成（Personalized Text-to-Speech）**经典语音 AI 任务，以阿里通义千问团队 2026 年 1 月开源的 **Qwen3-TTS** 模型为基线，依托学生自主录制的少量个人语音数据，完成从环境搭建、模型复现、个人音色微调到系统评测与优化的完整工程实践。核心目标分为 3 层：

**工程复现目标**：完整跑通 Qwen3-TTS 开源代码，完成模型下载、零样本推理（3 秒极速克隆）、个人语音数据采集与预处理、单说话人 SFT 微调训练、微调模型推理的全链路，在个人语音数据集上完成基线复现，输出可运行、可注释的完整代码工程；

**理论理解目标**：透彻掌握 Qwen3-TTS 端到端架构——12Hz 语音离散化 Tokenizer（将连续语音编码为离散码本 token）、离散多码本语言模型（Talker，直接建模文本→语音 token 序列）、Dual-Track 流式合成机制、说话人 embedding 注入与音色克隆机制；对比传统「声学模型 + 声码器」级联式 TTS（如 Tacotron+HiFiGAN）与「LM + DiT」两阶段方案，理解 Qwen3-TTS 统一端到端建模的优势，定位传统级联方案的信息瓶颈与误差累积缺陷；

**算法优化目标**：基于 Qwen3-TTS 基线方案提出至少 1 项有效改进方案并完成实验，从数据质量与增强、训练超参与策略、推理采样与控制、评测体系搭建等方向任选其一开展 ablation 消融实验，实现音色相似度（SIM）、内容准确率（WER/CER）、主观自然度（MOS）等指标的稳定提升（或在合成质量保持同水平的情况下，完成了更加轻量化的模型设计，减少了推理时间）；若所实现的方案未按预期取得对应效果，则给出精准靠谱的实验分析也可作为项目完成的评判（须经过项目组评审）。

## 课题实施思路建议

针对工程复现目标，建议认真阅读并学习课程资料包所提供的 Qwen3-TTS 代码仓库，对 12Hz Tokenizer 语音编解码器、JSONL 数据格式、PyTorch 训练框架、Qwen3-TTS 模型结构和 Loss、训练和推理代码等首先完成学习和 debug；其次，先在 `examples/` 目录下跑通预置音色推理（`test_model_12hz_custom_voice.py`）与 3 秒极速克隆（`test_model_12hz_base.py`），建立对模型输入输出的直观理解；随后进入 `finetuning/` 目录，依照 README 教程完成个人数据整理（JSONL 格式：每行含 `audio`、`text`、`ref_audio` 三字段）、数据预处理（`prepare_data.py` 提取 audio_codes）、单说话人 SFT 训练（`sft_12hz.py`）、微调模型推理测试的完整流程；最后，完成样本级的解码分步分析、频谱/音色可视化等。

**个人语音数据采集**是本课题区别于纯代码复现的核心环节，建议录制规模为每人 20~100 条语句（约 10~30 分钟净语音）；文本覆盖常用字/多音字、不同句长、陈述/疑问/感叹等多种语调，可选用标准 TTS 语料脚本（如中文标贝、AISHELL 文本）或自拟；录音规范为安静环境、单一麦克风、采样率 ≥16kHz（推荐 24kHz 单声道 wav）、避免爆音与长静音、单句独立成文件；数据整理为 JSONL，**`ref_audio` 强烈建议全数据集使用同一条以提升音色一致性与稳定性**；录音后必须逐句核对文本与音频严格对齐（可借助 ASR 辅助转写再人工校对），文本-音频不匹配是微调音色跑偏的头号原因。

基线模型选择：本课题统一使用 **Qwen3-TTS-12Hz-1.7B-Base** 进行 SFT 微调（官方当前仅支持单说话人微调，多说话人为后续特性），配合 **Qwen3-TTS-Tokenizer-12Hz** 做数据预处理。效果对标基线选择 **zero-shot 声音复刻系统**，即直接用 Qwen3-TTS-12Hz-1.7B-Base 的 3 秒极速克隆能力（提供一段参考音频，不做任何训练即合成目标音色），以此作为"未经微调"的对照，量化 SFT 微调相较 zero-shot 声音复刻在音色相似度与合成稳定性上的增益。

针对理论理解目标，建议首先通过书籍和博客学习补充相关的语音合成与语音表征基础知识（梅尔频谱、声码器、离散语音 token、自回归语音 LM），随后认真阅读课程资料包所提供的 Qwen3-TTS 技术报告（arXiv:2601.15621）以及相关工作（如 VALL-E、CosyVoice、SparkTTS 等离散 token 语音 LM），理解「语义 token + 声学 token」多码本建模范式，过程中针对缺失的知识点查漏补缺，必要时可对比传统 Tacotron/FastSpeech + HiFiGAN 级联方案以更深刻地理解统一端到端建模规避信息瓶颈与误差累积的优势；

针对算法优化目标，按照潜在优化点给予以下提示：

### 数据质量与增强优化

在原始录音基础上开展数据侧改进：静音裁剪与响度归一化、录音去噪与去混响、文本正则化（数字/单位/标点规范化）、音素对齐质检剔除坏样本；探索数据量-质量权衡（如对比 20 条 vs 50 条 vs 100 条的音色相似度曲线），或引入轻度语速/音高扰动做数据增强。通过扩充训练样本多样性，提升模型对不同语速、韵律、发音习惯的泛化能力，配套消融实验对比清洗前后、不同数据量下的 SIM/CER 指标差异。【提示：TTS 微调中"少而精"往往优于"多而杂"，坏样本（文本不对齐、噪音、爆音）的负面影响远大于其数量占比】

### 训练超参与策略优化

系统性调优学习率、epoch 数、batch size、梯度累积步数等训练超参，绘制训练 loss 与验证集指标随 epoch 的变化曲线，定位过拟合拐点（小数据 TTS SFT 极易过拟合到训练文本，表现为对训练集外新文本发音不稳、吞字或复读）；对比保存的不同 epoch checkpoint 在新文本上的合成效果，分析欠拟合（不像本人）到过拟合（吞字/卡顿/复读）的演变规律。

可探索 **LoRA（Low-Rank Adaptation）参数高效微调**方案：仅微调模型中的低秩适配矩阵而冻结主干参数，显著降低训练显存占用与可训练参数量（通常降至全量 SFT 的 1%~10%），同时保持接近全量微调的音色还原效果；对比 LoRA SFT vs 全量 SFT 在音色相似度、训练速度、显存占用、泛化能力等维度的 trade-off，分析 LoRA rank 超参（r=8/16/32/64）对效果的影响。也可探索仅微调部分模块（如仅 Talker 解码器）vs 全量微调、冻结 Tokenizer 等策略的效率-效果权衡，同步统计训练时间、显存占用、收敛速度。

### 推理采样与控制优化

调节推理阶段采样超参（`top_p`、`temperature`、`max_new_tokens` 等，均可透传给底层 `model.generate`）对合成稳定性与多样性的影响；探索参考音频（`ref_audio`）时长、质量、选择策略对最终音色相似度的影响；对合成音频做后处理（去除首尾静音、音量归一化、异常片段重采样），或引入解码器双向融合、多模型集成解码等机制强化推理效果。建立"合成失败率/重采样率"这一工程指标并通过采样策略调优降低失败率。

### 评测体系搭建与效果对标

搭建客观 + 主观评测流水线：**客观评测**用说话人验证模型（如 ERes2Net、WavLM-based Speaker Verification）计算合成音与真人音的余弦相似度（SIM）衡量音色还原度，用 ASR 模型（如 Paraformer、Whisper）转写合成音计算 CER/WER 衡量内容准确率；**主观评测**组织小范围 MOS 打分（自然度、相似度）或 ABX 听辨测试。以 zero-shot 声音复刻系统（1.7B-Base 的 3 秒极速克隆）为对照基线，量化 SFT 微调带来的相似度增益与质量提升。评测体系本身完备可靠、指标选择合理即是高质量交付物。

### 推理加速与部署优化（进阶方向）

测试 FlashAttention-2、bf16/fp16 混合精度、批量推理、KV Cache、流式合成等技术对首包延迟（latency）与吞吐量（throughput）的影响；统计推理显存占用、RTF（实时率，Real-Time Factor）、首包延迟等指标，分析合成质量与推理效率的 trade-off；也可探索模型量化、ONNX/TensorRT 导出等部署优化手段，在保持合成质量的前提下减少推理时间与资源占用。

## 课题验收方式

采用 1~3 人小组协作完成课题，每个小组到期需要提交代码工程、技术报告、答辩 PPT 3 部分。课题的验收和打分也是按照代码工程、技术报告、小组答辩三个维度综合打分，总分 100 分，并设置创新加分项，具体如下：

### 完整代码工程（30 分）

需要包含所有的数据处理、训练、推理、评测、可视化代码，并保留训练和推理的 log（训练 loss 曲线、各 epoch checkpoint、推理生成的音频样例），多组实验需要按文件夹整理整齐，整个代码工程要附上 README 说明（包括个人数据采集规模与规范、超参配置、复现步骤）；整体会按照实验的可复现性、代码的规范性、工程的整洁性和完备性等因素进行打分。个人语音数据可脱敏处理，但需在 README 中说明数据采集规模与规范。

### 实训技术报告（40 分）

需要包含任务概述与个性化语音合成研究现状、Qwen3-TTS 基线技术原理讲解（12Hz Tokenizer、离散多码本语言模型、Dual-Track 流式架构、音色克隆机制）、个人语音数据采集与处理方案详细介绍、自主改进方案详细介绍、全套对照实验、模块消融实验数据与可视化对比分析（训练 loss 曲线、SIM/CER 对比、频谱图对比、音色相似度主观听感等）、课题局限性、可拓展研究方向、参考文献等部分；中英文均可；整体会按照报告章节的完整性、技术方案的可读性、实验章节的完整性和分析的准确性、报告综合写作水平等因素进行打分。

### 小组答辩（30 分）

答辩环节旨在检验各小组对课题的整体把握深度、技术表达能力和临场应变能力。每个小组答辩时间控制在 15 分钟以内（汇报 10 分钟 + 提问 5 分钟），建议由 1 名主讲人进行汇报，组内其他成员共同参与问答环节。**鼓励答辩时现场播放微调前后的音色合成 demo 进行听感对比。** 答辩评分会重点考察汇报与表达、方案设计、技术理解深度、实验结果分析、团队协作与问答等方面。

### 创新加分项（最多 20 分）

- 每实现一项有实验验证的有效改进，或方案具原创性或提出新洞察，额外加 1~5 分；
- 搭建完备客观评测流水线（SIM/CER 自动化）或开展可视化分析并有深入见解，额外加 1~3 分；
- 提供 Web Demo（在线输入文本合成个人音色）、流式实时合成、训练监控等辅助工具，并能有效展示，额外加 1~3 分；
- 在多语言/跨语种合成、情感与风格控制、泛化性验证、效率极致优化、新任务适配等方面有额外有价值工作，额外加 1~3 分。

## 课题推荐的机器资源

本课题涉及多组对照/消融实验（不同数据量、不同超参、全量 vs LoRA SFT 等），需支持多个训练实验同步开展，因此按单实验所需训练卡数的约 3 倍配置机器资源。

**最低配置：**

- 显卡：3~4 张 NVIDIA RTX 3090/4090（24GB 显存），支持 3 组 1.7B-Base 单说话人 SFT 微调实验并行
- 服务器：CPU 16 核，内存 64GB，硬盘 200GB，可完成基线复现和基础消融实验。

**推荐配置：**

- 显卡：3~6 张 NVIDIA A100/A800（40GB/80GB 显存），或 8 张 RTX 3090/4090（24GB 显存）
- 服务器：CPU 32 核，内存 128GB，硬盘 400GB，支持更大 batch size、多组对照实验并行（多数据量/多超参组合/全量与 LoRA 对比）、流式推理压测与部署优化等复杂改进方案。

**显存参考**：Qwen3-TTS-12Hz-1.7B 全量 SFT + FlashAttention-2 + bf16 约需 20~40GB 显存（随 batch size 变化），LoRA SFT 可降至 12~20GB，纯推理约 6~10GB。

**软件环境**：Ubuntu 20.04/22.04，Python 3.12（官方推荐），PyTorch ≥2.1，CUDA 12.x，按 Qwen3-TTS 官方仓库 README 安装依赖（`pip install qwen-tts` 与源码 `pip install -e .`）；推荐安装 FlashAttention-2（`pip install flash-attn --no-build-isolation`）以降低显存占用和推理延迟。
